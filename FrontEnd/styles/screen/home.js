import {StyleSheet} from 'react-native';

const homeStyle = StyleSheet.create({
  container: {
    flex:1,
    borderWidth:1,
    borderColor:'red',
  },
  story: {
    borderWidth:1,
    borderColor:'blue',
  },
});
export default homeStyle;
